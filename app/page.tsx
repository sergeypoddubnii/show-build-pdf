'use client';

export default function Home() {

    return (
        <main>
            canvas
            <h1>Generate PDF with React-pdf</h1>
            <button onClick={() => {}}>Generate PDF</button>
        </main>
    )
}
