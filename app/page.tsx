'use client';

export default function Home() {

    return (
        <main>
            canvas
            <h1>Generate PDF from React</h1>
            <button onClick={() => {}}>Generate PDF</button>
        </main>
    )
}
